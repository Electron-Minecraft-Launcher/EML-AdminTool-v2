# craft-launch
> launch web site
# clone project
`git clone https://github.com/Electron-Minecraft-Launcher/EML-AdminTool-v2.git`
# config env.json
```
rename env.json.example to env.json
fill in the file
```
# start project
`npm i && npm start`
